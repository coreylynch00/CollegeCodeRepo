# Corey Lynch 7/11/19

outside_counter = int(input("Number you would like to multiply."))
inside_counter = (input("Number you would like to multiply to."))

for outside_counter in range(""):
    for inside_counter in range(""):
        product = outside_counter * inside_counter
        print(f"{outside_counter} x {inside_counter} = {product:2d}")
